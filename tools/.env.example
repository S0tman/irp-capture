# Collab — IRP-aware prompt launcher
# Transport is commodity. Protocol is sovereign.

# OpenAI API key (required unless using local model)
OPENAI_API_KEY=sk-proj-your_key_here

# Override model (default: gpt-4o)
# COLLAB_MODEL=gpt-4o-mini

# Override endpoint for local models (e.g. Ollama)
# COLLAB_API_BASE=http://localhost:11434/v1
